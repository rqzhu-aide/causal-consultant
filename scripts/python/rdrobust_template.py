"""Regression discontinuity template using rdrobust Python package."""
# from rdrobust import rdrobust, rdplot

# Y = df["Y"].to_numpy()
# X = df["running"].to_numpy()
# cutoff = 0

# rdplot(y=Y, x=X, c=cutoff)
# res = rdrobust(y=Y, x=X, c=cutoff)
# print(res)

# Add density/manipulation checks and bandwidth sensitivity.
